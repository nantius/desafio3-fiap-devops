apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: toggle
  region: us-east-1

nodeGroups:
  - name: worker-1
    instanceType: t3.large
    desiredCapacity: 1
    minSize: 1
    maxSize: 3
